# Agent Setup

This folder installs one packaged agent after infrastructure is ready.

## Current packaged agents
- `install-iris.md`
- `align-existing-iris-contextual-layer.md`

## Rule
Do not run an agent install playbook until the infrastructure verification playbook has passed or the human owner has explicitly accepted the remaining gaps.
